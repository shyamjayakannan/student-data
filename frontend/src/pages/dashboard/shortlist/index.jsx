import React from 'react'
import Layout from '../../../components/dashboard/layout'

const Shortlist = () => {
  return (
    <div>Shortlist</div>
  )
}

Shortlist.getLayout = (page) => <Layout>{page}</Layout>;

export default Shortlist